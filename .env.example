# Smoothie dashboard
# Password di accesso a /smoothie/
SMOOTHIE_PASSWORD=change-me
# Chiave HMAC per firmare il cookie di sessione (32+ byte casuali)
# Genera con: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
SMOOTHIE_SECRET=change-me-to-a-long-random-hex-string
